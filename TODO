TODOS:

	☐ Store users by Twitch ID in case they change their names
	☐ Move all commands to a CommandHandler?
	☐ Check for streamer using `badges.broadcaster`
	☐ Make `command-prefix` a settings thing? Example ! # $ &
	☐ Abstract response builders to it's own module
	[x] Make GameHelper not a class
	☐ Make Store not a class
	☐ Make the game also work on challenges?
	☐ Make it possible to open challenges links from the game itself
		This 2 changes above should make it possible so people that
		doesn't have pro can still use chatguessr.

